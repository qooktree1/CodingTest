const solution = (priorities, location) => {
    const waitingQ = priorities.map((priority, index) => [priority, index])
    const N = priorities.length
    // while (true) {
    //     priorities.pop()
    // }
    console.log(waitingQ)
    
}

// A, B, C, D
// C D A B